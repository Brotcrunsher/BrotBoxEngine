#include "BBE/Color.h"

bbe::Color::Color()
	: r(0), g(0), b(0), a(1)
{
	//UNTESTED
}

bbe::Color::Color(float r, float g, float b)
	: r(r), g(g), b(b), a(1)
{
	//UNTESTED
}

bbe::Color::Color(float r, float g, float b, float a)
	: r(r), g(g), b(b), a(a)
{
	//UNTESTED
}